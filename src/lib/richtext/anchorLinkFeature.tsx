'use client'
/*
import {
  createLexicalPlugin,
  createLexicalRichTextFeature,
} from '@payloadcms/richtext-lexical'
*/
/**
 * Very small “feature” that lets editors insert an anchor-link
 * (text that links to `#some-id` on the same page).
 *
 * You’ll wire the command up to a custom toolbar button later.
 */
/*
export const anchorLinkFeature = createLexicalRichTextFeature({
  feature: {
    // name that appears in the Payload UI -- optional
    slug: 'anchor-link',
    // the Lexical plugin itself (does nothing yet, but must exist)
    plugins() {
      return [createLexicalPlugin({})]
    },
  },
})

*/